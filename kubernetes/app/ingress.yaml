apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: portfolio-server-ingress
  namespace: portfolio
  annotations:
    kubernetes.io/ingress.global-static-ip-name: portfolio-server-ip
  labels:
    app: portfolio-server
spec:
  backend:
    servicePort: 8080
    serviceName: portfolio-server-service
